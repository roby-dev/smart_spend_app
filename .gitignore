# Umbrella monorepo ignores.
# Each subproject keeps its own .gitignore:
#   smart_spend_app_mobile/.gitignore  (Flutter)
#   smart_spend_backend/.gitignore     (NestJS)

# Agent / IDE tooling
.antigravitycli/
.atl/

# OS
.DS_Store
Thumbs.db
